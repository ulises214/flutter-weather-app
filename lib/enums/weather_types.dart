part of enums_library;

enum WeatherTypes { CLEAR, THUNDERSTORM, DRIZZLE, RAIN, SNOW, CLOUDS }
enum ClearTypes { DAY, NIGHT }
enum ThunderstormTypes { DEFAULT }
enum DrizzleTypes { DEFAULT }
enum RainTypes { LIGHT, MODERATE, HEAVY, SHOWER, RAGGED }
enum SnowTypes { DEFAULT }
enum CloudsTypes { DAY, NIGHT, BROKEN }
